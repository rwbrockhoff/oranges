const initialState = {
    user: {},
    scores: [],
    judge: false,
    aCard: [],
    aCards: [],
    sCards: [],
    qCard: {}
}

export default (state = initialState, action) => {
  switch (action.type) {


  default:
    return state
  }
}
